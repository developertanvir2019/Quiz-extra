import React from 'react';

const QuizCard = ({ ques }) => {
    console.log(ques)
    const { question, options, correctAnswer } = ques;
    return (
        <div>

        </div>
    );
};

export default QuizCard;